import AppPageLoader from "@/components/modules/shared/AppPageLoader";

export default function ProviderLoading() {
  return <AppPageLoader title="Loading provider workspace" subtitle="Getting your menu, categories, and live orders..." cardCount={6} />;
}
