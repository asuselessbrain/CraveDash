import AppPageLoader from "@/components/modules/shared/AppPageLoader";

export default function ProviderCuisinesLoading() {
  return <AppPageLoader title="Loading cuisines" subtitle="Preparing cuisine performance and controls..." cardCount={8} />;
}
