import AppPageLoader from "@/components/modules/shared/AppPageLoader";

export default function RootAppLoading() {
  return <AppPageLoader title="Starting CraveDash" subtitle="Setting up your workspace..." cardCount={3} />;
}
